name: Prueba shell

on: [push]

jobs:
  run-shell-command:  
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Prueba de workflow"
      - name: multiline script
        run: |
        